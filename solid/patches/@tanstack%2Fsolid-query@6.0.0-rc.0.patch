diff --git a/build/dev.cjs b/build/dev.cjs
index a01af1aa0b8f819d864cf8f9b217aefea301640e..ce249fe24ad2b2ce3a2eda98f45349fb54ab6e3f 100644
--- a/build/dev.cjs
+++ b/build/dev.cjs
@@ -150,6 +150,9 @@ function createHydrationCoordinator(client) {
         waiters.set(queryHash, list);
       }
       list.push(callback);
+    },
+    isQueryPrimed(queryHash) {
+      return channelDone || applied.has(queryHash);
     }
   };
 }
@@ -336,8 +339,7 @@ function useBaseQuery(options, Observer, queryClient) {
       unsubscribe = createClientSubscriber();
     }
   };
-  const scheduleHydratedAttach = () => {
-    const queryHash = solidJs.untrack(() => observer.getCurrentQuery().queryHash);
+  const scheduleHydratedAttach = (queryHash) => {
     if (coordinator) {
       coordinator.whenQueryPrimed(queryHash, attachHydratedSubscriber);
     } else {
@@ -348,6 +350,8 @@ function useBaseQuery(options, Observer, queryClient) {
   const [queryResource] = solidJs.createSignal(() => {
     const opts = trackedDefaultedOptions();
     const restoring = isRestoring();
+    const queryHash = solidJs.untrack(() => observer.getCurrentQuery().queryHash);
+    const streamHydrationPending = coordinator !== null && !coordinator.isQueryPrimed(queryHash);
     if (isServer2) {
       const cached = observer.getOptimisticResult(opts);
       if (!cached.isLoading) {
@@ -369,7 +373,7 @@ function useBaseQuery(options, Observer, queryClient) {
         unsubscribe = createServerSubscriber((data) => {
           resolve(data);
         }, reject);
-      } else if (!unsubscribe && !restoring) {
+      } else if (!unsubscribe && !restoring && !streamHydrationPending) {
         unsubscribe = createClientSubscriber();
       }
       const currentResult = observer.getOptimisticResult(opts);
@@ -398,8 +402,8 @@ function useBaseQuery(options, Observer, queryClient) {
         );
       }
     });
-    if (!isServer2 && !replayProbe.executorRan) {
-      scheduleHydratedAttach();
+    if (!isServer2 && (streamHydrationPending || !replayProbe.executorRan)) {
+      scheduleHydratedAttach(queryHash);
     }
     return resource;
   });
diff --git a/build/dev.js b/build/dev.js
index be882a4bddf3b59309f76d368670c693071442f5..d2fc7c610b66886386b53cf1c8cc8d7fab100124 100644
--- a/build/dev.js
+++ b/build/dev.js
@@ -149,6 +149,9 @@ function createHydrationCoordinator(client) {
         waiters.set(queryHash, list);
       }
       list.push(callback);
+    },
+    isQueryPrimed(queryHash) {
+      return channelDone || applied.has(queryHash);
     }
   };
 }
@@ -335,8 +338,7 @@ function useBaseQuery(options, Observer, queryClient) {
       unsubscribe = createClientSubscriber();
     }
   };
-  const scheduleHydratedAttach = () => {
-    const queryHash = untrack(() => observer.getCurrentQuery().queryHash);
+  const scheduleHydratedAttach = (queryHash) => {
     if (coordinator) {
       coordinator.whenQueryPrimed(queryHash, attachHydratedSubscriber);
     } else {
@@ -347,6 +349,8 @@ function useBaseQuery(options, Observer, queryClient) {
   const [queryResource] = createSignal(() => {
     const opts = trackedDefaultedOptions();
     const restoring = isRestoring();
+    const queryHash = untrack(() => observer.getCurrentQuery().queryHash);
+    const streamHydrationPending = coordinator !== null && !coordinator.isQueryPrimed(queryHash);
     if (isServer2) {
       const cached = observer.getOptimisticResult(opts);
       if (!cached.isLoading) {
@@ -368,7 +372,7 @@ function useBaseQuery(options, Observer, queryClient) {
         unsubscribe = createServerSubscriber((data) => {
           resolve(data);
         }, reject);
-      } else if (!unsubscribe && !restoring) {
+      } else if (!unsubscribe && !restoring && !streamHydrationPending) {
         unsubscribe = createClientSubscriber();
       }
       const currentResult = observer.getOptimisticResult(opts);
@@ -397,8 +401,8 @@ function useBaseQuery(options, Observer, queryClient) {
         );
       }
     });
-    if (!isServer2 && !replayProbe.executorRan) {
-      scheduleHydratedAttach();
+    if (!isServer2 && (streamHydrationPending || !replayProbe.executorRan)) {
+      scheduleHydratedAttach(queryHash);
     }
     return resource;
   });
diff --git a/build/index.cjs b/build/index.cjs
index e1f5f247bc83261b10ac71be7bc72c68f726c6df..dceb8fb98cdf51036215400a86d51075c09355c1 100644
--- a/build/index.cjs
+++ b/build/index.cjs
@@ -150,6 +150,9 @@ function createHydrationCoordinator(client) {
         waiters.set(queryHash, list);
       }
       list.push(callback);
+    },
+    isQueryPrimed(queryHash) {
+      return channelDone || applied.has(queryHash);
     }
   };
 }
@@ -327,8 +330,7 @@ function useBaseQuery(options, Observer, queryClient) {
       unsubscribe = createClientSubscriber();
     }
   };
-  const scheduleHydratedAttach = () => {
-    const queryHash = solidJs.untrack(() => observer.getCurrentQuery().queryHash);
+  const scheduleHydratedAttach = (queryHash) => {
     if (coordinator) {
       coordinator.whenQueryPrimed(queryHash, attachHydratedSubscriber);
     } else {
@@ -339,6 +341,8 @@ function useBaseQuery(options, Observer, queryClient) {
   const [queryResource] = solidJs.createSignal(() => {
     const opts = trackedDefaultedOptions();
     const restoring = isRestoring();
+    const queryHash = solidJs.untrack(() => observer.getCurrentQuery().queryHash);
+    const streamHydrationPending = coordinator !== null && !coordinator.isQueryPrimed(queryHash);
     if (isServer2) {
       const cached = observer.getOptimisticResult(opts);
       if (!cached.isLoading) {
@@ -360,7 +364,7 @@ function useBaseQuery(options, Observer, queryClient) {
         unsubscribe = createServerSubscriber((data) => {
           resolve(data);
         }, reject);
-      } else if (!unsubscribe && !restoring) {
+      } else if (!unsubscribe && !restoring && !streamHydrationPending) {
         unsubscribe = createClientSubscriber();
       }
       const currentResult = observer.getOptimisticResult(opts);
@@ -389,8 +393,8 @@ function useBaseQuery(options, Observer, queryClient) {
         );
       }
     });
-    if (!isServer2 && !replayProbe.executorRan) {
-      scheduleHydratedAttach();
+    if (!isServer2 && (streamHydrationPending || !replayProbe.executorRan)) {
+      scheduleHydratedAttach(queryHash);
     }
     return resource;
   });
diff --git a/build/index.js b/build/index.js
index 73fe3b7df8d758c71068e46126c3085fa935bdb0..95ac2525deff538989081f2072ebcfc523b9bff3 100644
--- a/build/index.js
+++ b/build/index.js
@@ -149,6 +149,9 @@ function createHydrationCoordinator(client) {
         waiters.set(queryHash, list);
       }
       list.push(callback);
+    },
+    isQueryPrimed(queryHash) {
+      return channelDone || applied.has(queryHash);
     }
   };
 }
@@ -326,8 +329,7 @@ function useBaseQuery(options, Observer, queryClient) {
       unsubscribe = createClientSubscriber();
     }
   };
-  const scheduleHydratedAttach = () => {
-    const queryHash = untrack(() => observer.getCurrentQuery().queryHash);
+  const scheduleHydratedAttach = (queryHash) => {
     if (coordinator) {
       coordinator.whenQueryPrimed(queryHash, attachHydratedSubscriber);
     } else {
@@ -338,6 +340,8 @@ function useBaseQuery(options, Observer, queryClient) {
   const [queryResource] = createSignal(() => {
     const opts = trackedDefaultedOptions();
     const restoring = isRestoring();
+    const queryHash = untrack(() => observer.getCurrentQuery().queryHash);
+    const streamHydrationPending = coordinator !== null && !coordinator.isQueryPrimed(queryHash);
     if (isServer2) {
       const cached = observer.getOptimisticResult(opts);
       if (!cached.isLoading) {
@@ -359,7 +363,7 @@ function useBaseQuery(options, Observer, queryClient) {
         unsubscribe = createServerSubscriber((data) => {
           resolve(data);
         }, reject);
-      } else if (!unsubscribe && !restoring) {
+      } else if (!unsubscribe && !restoring && !streamHydrationPending) {
         unsubscribe = createClientSubscriber();
       }
       const currentResult = observer.getOptimisticResult(opts);
@@ -388,8 +392,8 @@ function useBaseQuery(options, Observer, queryClient) {
         );
       }
     });
-    if (!isServer2 && !replayProbe.executorRan) {
-      scheduleHydratedAttach();
+    if (!isServer2 && (streamHydrationPending || !replayProbe.executorRan)) {
+      scheduleHydratedAttach(queryHash);
     }
     return resource;
   });
diff --git a/src/hydrationChannel.ts b/src/hydrationChannel.ts
index 75bd4359cc859992fa85d062d4e6c9a87b33f592..d845622db6e679ec0e5968d730a90af579251145 100644
--- a/src/hydrationChannel.ts
+++ b/src/hydrationChannel.ts
@@ -190,6 +190,11 @@ interface HydrationCoordinator {
    * dehydrated, so their components must not wait forever).
    */
   whenQueryPrimed: (queryHash: string, callback: () => void) => void
+  /**
+   * Whether the stream has applied this query's entry, or has completed
+   * without one.
+   */
+  isQueryPrimed: (queryHash: string) => boolean
 }
 
 /**
@@ -253,6 +258,9 @@ export function createHydrationCoordinator(
       }
       list.push(callback)
     },
+    isQueryPrimed(queryHash) {
+      return channelDone || applied.has(queryHash)
+    },
   }
 }
 
diff --git a/src/useBaseQuery.ts b/src/useBaseQuery.ts
index 94e3775f4eec774cc5cc083f1ebf161fc891946b..d2cbcde182249ca1f52adcbd3438c86105fad38b 100644
--- a/src/useBaseQuery.ts
+++ b/src/useBaseQuery.ts
@@ -311,8 +311,7 @@ export function useBaseQuery<
       unsubscribe = createClientSubscriber()
     }
   }
-  const scheduleHydratedAttach = () => {
-    const queryHash = untrack(() => observer.getCurrentQuery().queryHash)
+  const scheduleHydratedAttach = (queryHash: string) => {
     if (coordinator) {
       coordinator.whenQueryPrimed(queryHash, attachHydratedSubscriber)
     } else {
@@ -338,6 +337,9 @@ export function useBaseQuery<
     const opts = trackedDefaultedOptions()
     // Read isRestoring unconditionally so the memo re-runs when it changes
     const restoring = isRestoring()
+    const queryHash = untrack(() => observer.getCurrentQuery().queryHash)
+    const streamHydrationPending =
+      coordinator !== null && !coordinator.isQueryPrimed(queryHash)
 
     if (isServer) {
       // On retry passes (after the streaming Loading boundary awaits a
@@ -372,7 +374,11 @@ export function useBaseQuery<
         unsubscribe = createServerSubscriber((data) => {
           resolve(data as ResourceData)
         }, reject)
-      } else if (!unsubscribe && !restoring) {
+      } else if (
+        !unsubscribe &&
+        !restoring &&
+        !streamHydrationPending
+      ) {
         unsubscribe = createClientSubscriber()
       }
       // Use getOptimisticResult instead of updateResult to keep the memo
@@ -418,11 +424,11 @@ export function useBaseQuery<
       }
     })
 
-    if (!isServer && !replayProbe.executorRan) {
-      // Hydration replay: `Promise` was mocked and the executor above never
-      // ran, so no subscriber was created. Schedule the attach through the
-      // provider's hydration coordinator (see scheduleHydratedAttach).
-      scheduleHydratedAttach()
+    if (!isServer && (streamHydrationPending || !replayProbe.executorRan)) {
+      // A stream-hydrated query must wait for its cache entry before attaching
+      // even when Promise runs its executor during hydration. The replay path
+      // also schedules here because its executor did not create a subscriber.
+      scheduleHydratedAttach(queryHash)
     }
 
     return resource
