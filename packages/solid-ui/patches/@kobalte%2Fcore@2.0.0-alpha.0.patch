diff --git a/dist/button/DvspU6cJ.jsx b/dist/button/DvspU6cJ.jsx
index 5000f2871ccda16662437b482ecb25609fa1fd2f..cb36a8cdbb91cad4dbae069606738a204f8af1ec 100644
--- a/dist/button/DvspU6cJ.jsx
+++ b/dist/button/DvspU6cJ.jsx
@@ -36,7 +36,7 @@ function isButton(element) {
 function ButtonRoot(props) {
 	const [ref, setRef] = createSignal(void 0, { ownedWrite: true });
 	const mergedProps = merge({ type: "button" }, props);
-	const others = omit(mergedProps, "ref", "type", "disabled");
+	const others = omit(mergedProps, "ref", "type", "disabled", "children", "as");
 	const tagName = createTagName(ref, () => "button");
 	const isNativeButton = createMemo(() => {
 		const elementTagName = tagName();
@@ -52,7 +52,8 @@ function ButtonRoot(props) {
 	const isNativeLink = createMemo(() => {
 		return tagName() === "a" && ref()?.getAttribute("href") != null;
 	});
-	return <Polymorphic as="button" ref={[setRef, mergedProps.ref]} type={isNativeButton() || isNativeInput() ? mergedProps.type : void 0} role={!isNativeButton() && !isNativeLink() ? "button" : void 0} tabindex={!isNativeButton() && !isNativeLink() && !mergedProps.disabled ? 0 : void 0} disabled={isNativeButton() || isNativeInput() ? mergedProps.disabled : void 0} aria-disabled={!isNativeButton() && !isNativeInput() && mergedProps.disabled ? "true" : void 0} data-disabled={mergedProps.disabled ? "" : void 0} {...others} />;
+	if (mergedProps.as && mergedProps.as !== "button") return <Polymorphic as={mergedProps.as} ref={[setRef, mergedProps.ref]} type={isNativeButton() || isNativeInput() ? mergedProps.type : void 0} role={!isNativeButton() && !isNativeLink() ? "button" : void 0} tabindex={!isNativeButton() && !isNativeLink() && !mergedProps.disabled ? 0 : void 0} disabled={isNativeButton() || isNativeInput() ? mergedProps.disabled : void 0} aria-disabled={!isNativeButton() && !isNativeInput() && mergedProps.disabled ? "true" : void 0} data-disabled={mergedProps.disabled ? "" : void 0} {...others}>{mergedProps.children}</Polymorphic>;
+	return <button {...others} ref={[setRef, mergedProps.ref]} type={mergedProps.type} disabled={mergedProps.disabled}>{mergedProps.children}</button>;
 }
 //#endregion
 //#region src/button/index.tsx
diff --git a/dist/collapsible/BZoV6_oa.jsx b/dist/collapsible/BZoV6_oa.jsx
index 8a87f9d97da655fbf791df1edf12aa60a849e0fb..3ddbf5a1fa534b354060e4994b118b9ea62f7343 100644
--- a/dist/collapsible/BZoV6_oa.jsx
+++ b/dist/collapsible/BZoV6_oa.jsx
@@ -25,8 +25,8 @@ function CollapsibleContent(props) {
 	const mergedProps = merge({ id: context.generateId("content") }, props);
 	const others = omit(mergedProps, "ref", "id", "style");
 	const { isMounted: present } = createPresence(() => context.shouldMount() || void 0, { transitionDuration: 0 });
-	const [height, setHeight] = createSignal(0);
-	const [width, setWidth] = createSignal(0);
+	const [height, setHeight] = createSignal(0, { ownedWrite: true });
+	const [width, setWidth] = createSignal(0, { ownedWrite: true });
 	const isOpen = () => context.isOpen() || present();
 	let isMountAnimationPrevented = isOpen();
 	onSettled(() => {
diff --git a/dist/combobox-base/BFUCtj_r.jsx b/dist/combobox-base/BFUCtj_r.jsx
index b07ca00acb0bac67977b00a8d747032ef016105c..176ba4e4830ec4045c2128c4836d8257d73fe7a7 100644
--- a/dist/combobox-base/BFUCtj_r.jsx
+++ b/dist/combobox-base/BFUCtj_r.jsx
@@ -323,7 +323,7 @@ function ComboboxBase(props) {
 	const [focusStrategy, setFocusStrategy] = createSignal(false);
 	const [isInputFocused, setIsInputFocusedState] = createSignal(false);
 	const [showAllOptions, setShowAllOptions] = createSignal(false);
-	const [lastDisplayedOptions, setLastDisplayedOptions] = createSignal(mergedProps.options);
+	const [lastDisplayedOptions, setLastDisplayedOptions] = createSignal(mergedProps.options, { ownedWrite: true });
 	const disclosureState = createDisclosureState({
 		open: () => mergedProps.open,
 		defaultOpen: () => mergedProps.defaultOpen,
@@ -396,12 +396,14 @@ function ComboboxBase(props) {
 	};
 	const listState = createListState({
 		selectedKeys: () => {
-			if (mergedProps.value != null) return mergedProps.value.map(getOptionValue);
-			return mergedProps.value;
+			const rawValue = mergedProps.value;
+			if (rawValue == null) return rawValue;
+			return (Array.isArray(rawValue) ? rawValue : [rawValue]).map(getOptionValue);
 		},
 		defaultSelectedKeys: () => {
-			if (mergedProps.defaultValue != null) return mergedProps.defaultValue.map(getOptionValue);
-			return mergedProps.defaultValue;
+			const rawValue = mergedProps.defaultValue;
+			if (rawValue == null) return rawValue;
+			return (Array.isArray(rawValue) ? rawValue : [rawValue]).map(getOptionValue);
 		},
 		onSelectionChange: (selectedKeys) => {
 			mergedProps.onChange?.(getOptionsFromValues(selectedKeys));
diff --git a/dist/combobox-base/pqkzvj0_.js b/dist/combobox-base/pqkzvj0_.js
index 839d79ad6ae08342ddca0b8bf2782f81e77fb66c..a83c8095036d67af09cd3e0d9a9a24c0452ff0ca 100644
--- a/dist/combobox-base/pqkzvj0_.js
+++ b/dist/combobox-base/pqkzvj0_.js
@@ -460,7 +460,7 @@ function ComboboxBase(props) {
 	const [focusStrategy, setFocusStrategy] = createSignal(false);
 	const [isInputFocused, setIsInputFocusedState] = createSignal(false);
 	const [showAllOptions, setShowAllOptions] = createSignal(false);
-	const [lastDisplayedOptions, setLastDisplayedOptions] = createSignal(mergedProps.options);
+	const [lastDisplayedOptions, setLastDisplayedOptions] = createSignal(mergedProps.options, { ownedWrite: true });
 	const disclosureState = createDisclosureState({
 		open: () => mergedProps.open,
 		defaultOpen: () => mergedProps.defaultOpen,
@@ -533,12 +533,14 @@ function ComboboxBase(props) {
 	};
 	const listState = createListState({
 		selectedKeys: () => {
-			if (mergedProps.value != null) return mergedProps.value.map(getOptionValue);
-			return mergedProps.value;
+			const rawValue = mergedProps.value;
+			if (rawValue == null) return rawValue;
+			return (Array.isArray(rawValue) ? rawValue : [rawValue]).map(getOptionValue);
 		},
 		defaultSelectedKeys: () => {
-			if (mergedProps.defaultValue != null) return mergedProps.defaultValue.map(getOptionValue);
-			return mergedProps.defaultValue;
+			const rawValue = mergedProps.defaultValue;
+			if (rawValue == null) return rawValue;
+			return (Array.isArray(rawValue) ? rawValue : [rawValue]).map(getOptionValue);
 		},
 		onSelectionChange: (selectedKeys) => {
 			mergedProps.onChange?.(getOptionsFromValues(selectedKeys));
diff --git a/dist/create-dom-collection/OK7882aq.jsx b/dist/create-dom-collection/OK7882aq.jsx
index 6b8a3617f27946fec9535b14e108400de48a4bf2..5f82241798c3a67d64be6c373b4718551418cb9b 100644
--- a/dist/create-dom-collection/OK7882aq.jsx
+++ b/dist/create-dom-collection/OK7882aq.jsx
@@ -91,7 +91,7 @@ function createSortBasedOnDOMPosition(items, setItems) {
 //#endregion
 //#region src/primitives/create-dom-collection/create-dom-collection.ts
 function createDomCollection(props = {}) {
-	const [items, setItems] = createSignal([]);
+	const [items, setItems] = createSignal([], { ownedWrite: true });
 	createSortBasedOnDOMPosition(items, setItems);
 	createEffect(() => items(), (currentItems) => {
 		props.onItemsChange?.(currentItems);
diff --git a/dist/create-list-state/CgkAj-2j.jsx b/dist/create-list-state/CgkAj-2j.jsx
index 459d60819480f18a86c3b292291761620c37ffad..4facabee801fd50f0287ebddcc4f4d9272225ddc 100644
--- a/dist/create-list-state/CgkAj-2j.jsx
+++ b/dist/create-list-state/CgkAj-2j.jsx
@@ -65,7 +65,7 @@ function createMultipleSelectionState(props) {
 		selectionBehavior: "toggle"
 	}, props);
 	const [isFocused, setFocused] = createSignal(false);
-	const [focusedKey, setFocusedKey] = createSignal();
+	const [focusedKey, setFocusedKey] = createSignal(undefined, { ownedWrite: true });
 	const [selectedKeys, _setSelectedKeys] = createControllableSelectionSignal({
 		value: createMemo(() => {
 			const selection = access(mergedProps.selectedKeys);
@@ -79,7 +79,7 @@ function createMultipleSelectionState(props) {
 		}),
 		onChange: (value) => mergedProps.onSelectionChange?.(value)
 	});
-	const [selectionBehavior, setSelectionBehavior] = createSignal(access(mergedProps.selectionBehavior));
+	const [selectionBehavior, setSelectionBehavior] = createSignal(access(mergedProps.selectionBehavior), { ownedWrite: true });
 	const selectionMode = () => access(mergedProps.selectionMode);
 	const disallowEmptySelection = () => access(mergedProps.disallowEmptySelection) ?? false;
 	const setSelectedKeys = (keys) => {
diff --git a/dist/polymorphic/index.jsx b/dist/polymorphic/index.jsx
index 0a0891f3cbb8704639ccf0fa979179117c26505a..1b0fd378f2317dcfed732f357335764ddbe6874f 100644
--- a/dist/polymorphic/index.jsx
+++ b/dist/polymorphic/index.jsx
@@ -5,9 +5,9 @@ import { Dynamic } from "@solidjs/web";
 * A utility component that render its `as` prop.
 */
 function Polymorphic(props) {
-	const others = omit(props, "as");
+	const others = omit(props, "as", "children");
 	if (!untrack(() => props.as)) throw new Error("[kobalte]: Polymorphic is missing the required `as` prop.");
-	return <Dynamic {...others} component={props.as} />;
+	return <Dynamic {...others} component={props.as}>{props.children}</Dynamic>;
 }
 //#endregion
 export { Polymorphic };
diff --git a/dist/select/C2EhSCqp.jsx b/dist/select/C2EhSCqp.jsx
index c59f01b8f5d79f3d349b315a7b131100cf2a0033..a619251bdd6041b16b43b931e75f4c27c80cf90d 100644
--- a/dist/select/C2EhSCqp.jsx
+++ b/dist/select/C2EhSCqp.jsx
@@ -204,12 +204,14 @@ function SelectBase(props) {
 	});
 	const listState = createListState({
 		selectedKeys: () => {
-			if (mergedProps.value != null) return mergedProps.value.map(getOptionValue);
-			return mergedProps.value;
+			const rawValue = mergedProps.value;
+			if (rawValue == null) return rawValue;
+			return (Array.isArray(rawValue) ? rawValue : [rawValue]).map(getOptionValue);
 		},
 		defaultSelectedKeys: () => {
-			if (mergedProps.defaultValue != null) return mergedProps.defaultValue.map(getOptionValue);
-			return mergedProps.defaultValue;
+			const rawValue = mergedProps.defaultValue;
+			if (rawValue == null) return rawValue;
+			return (Array.isArray(rawValue) ? rawValue : [rawValue]).map(getOptionValue);
 		},
 		onSelectionChange: (selectedKeys) => {
 			mergedProps.onChange?.(getOptionsFromValues(selectedKeys));
@@ -369,7 +371,9 @@ function SelectTrigger(props) {
 		onTypeSelect: (key) => selectionManager().select(key)
 	});
 	const ariaLabelledBy = () => {
-		return [context.listboxAriaLabelledBy(), context.valueId()].filter(Boolean).join(" ") || void 0;
+		if (fieldProps.ariaLabel() && !fieldProps.ariaLabelledBy()) return void 0;
+		const labels = [fieldProps.ariaLabelledBy(), context.valueId()].filter(Boolean);
+		return labels.join(" ") || void 0;
 	};
 	const onPointerDown = (e) => {
 		callHandler(e, mergedProps.onPointerDown);
diff --git a/dist/select/DuH3IApm.js b/dist/select/DuH3IApm.js
index f74d54461295486ec063fd32d5dfe5489be7a339..2a82cabe9fdf04a9d790065552ced636074de66e 100644
--- a/dist/select/DuH3IApm.js
+++ b/dist/select/DuH3IApm.js
@@ -284,12 +284,14 @@ function SelectBase(props) {
 	});
 	const listState = createListState({
 		selectedKeys: () => {
-			if (mergedProps.value != null) return mergedProps.value.map(getOptionValue);
-			return mergedProps.value;
+			const rawValue = mergedProps.value;
+			if (rawValue == null) return rawValue;
+			return (Array.isArray(rawValue) ? rawValue : [rawValue]).map(getOptionValue);
 		},
 		defaultSelectedKeys: () => {
-			if (mergedProps.defaultValue != null) return mergedProps.defaultValue.map(getOptionValue);
-			return mergedProps.defaultValue;
+			const rawValue = mergedProps.defaultValue;
+			if (rawValue == null) return rawValue;
+			return (Array.isArray(rawValue) ? rawValue : [rawValue]).map(getOptionValue);
 		},
 		onSelectionChange: (selectedKeys) => {
 			mergedProps.onChange?.(getOptionsFromValues(selectedKeys));
@@ -475,7 +477,9 @@ function SelectTrigger(props) {
 		onTypeSelect: (key) => selectionManager().select(key)
 	});
 	const ariaLabelledBy = () => {
-		return [context.listboxAriaLabelledBy(), context.valueId()].filter(Boolean).join(" ") || void 0;
+		if (fieldProps.ariaLabel() && !fieldProps.ariaLabelledBy()) return void 0;
+		const labels = [fieldProps.ariaLabelledBy(), context.valueId()].filter(Boolean);
+		return labels.join(" ") || void 0;
 	};
 	const onPointerDown = (e) => {
 		callHandler(e, mergedProps.onPointerDown);
diff --git a/dist/slider/DP6fmlOK.jsx b/dist/slider/DP6fmlOK.jsx
index 99232b6df91ac600326db388fdd42a96e396c343..9e970b682f7b0d87ab27ea578b429b61bb8215ca 100644
--- a/dist/slider/DP6fmlOK.jsx
+++ b/dist/slider/DP6fmlOK.jsx
@@ -118,7 +118,7 @@ function SliderThumb(props) {
 			<Polymorphic as="span" ref={[setRef, mergedProps.ref]} role="slider" id={fieldProps.id()} tabindex={context.state.isDisabled() ? void 0 : 0} style={combineStyle({
 		display: value() === void 0 ? "none" : void 0,
 		position: "absolute",
-		[context.startEdge()]: `calc(${position() * 100}%)`,
+		[context.startEdge()]: value() === void 0 ? void 0 : `calc(${position() * 100}%)`,
 		transform: transform(),
 		"touch-action": "none"
 	}, mergedProps.style)} aria-valuetext={context.state.getThumbValueLabel(index())} aria-valuemin={context.minValue()} aria-valuenow={value()} aria-valuemax={context.maxValue()} aria-orientation={context.state.orientation()} aria-label={fieldProps.ariaLabel()} aria-labelledby={fieldProps.ariaLabelledBy()} aria-describedby={fieldProps.ariaDescribedBy()} onKeyDown={onKeyDown} onPointerDown={onPointerDown} onPointerMove={onPointerMove} onPointerUp={onPointerUp} onFocus={onFocus} onBlur={onBlur} {...context.dataset()} {...others} />
diff --git a/dist/tabs/CezyXMlG.jsx b/dist/tabs/CezyXMlG.jsx
index 6386365bd6e1ef3d76be952cd214f54dfa53e937..1b3565a0ac78bfe5b674026156be0959015c52f0 100644
--- a/dist/tabs/CezyXMlG.jsx
+++ b/dist/tabs/CezyXMlG.jsx
@@ -65,10 +65,10 @@ function useTabsContext() {
 * Contains the content associated with a tab trigger.
 */
 function TabsContent(props) {
-	const [ref, setRef] = createSignal();
+const [ref, setRef] = createSignal(void 0, { ownedWrite: true });
 	const context = useTabsContext();
 	const others = omit(props, "ref", "id", "value", "forceMount");
-	const [tabIndex, setTabIndex] = createSignal(0);
+const [tabIndex, setTabIndex] = createSignal(0, { ownedWrite: true });
 	const id = () => props.id ?? context.generateContentId(props.value);
 	const isSelected = () => context.listState().selectedKey() === props.value;
 	const { isMounted: present } = createPresence(() => props.forceMount || isSelected() || void 0, { transitionDuration: 0 });
@@ -107,7 +107,7 @@ function TabsIndicator(props) {
 	const [style, setStyle] = createSignal({
 		width: void 0,
 		height: void 0
-	});
+	}, { ownedWrite: true });
 	const { direction } = useLocale();
 	const computeStyle = () => {
 		const selectedTab = context.selectedTab();
@@ -135,7 +135,7 @@ function TabsIndicator(props) {
 	], () => {
 		computeStyle();
 	}, { defer: true });
-	const [resizing, setResizing] = createSignal(false);
+	const [resizing, setResizing] = createSignal(false, { ownedWrite: true });
 	let timeout = null;
 	let prevTarget = null;
 	createResizeObserver(context.selectedTab, (_, t) => {
@@ -192,8 +192,8 @@ function TabsRoot(props) {
 		activationMode: "automatic"
 	}, props);
 	const others = omit(mergedProps, "value", "defaultValue", "onChange", "orientation", "activationMode", "disabled");
-	const [items, setItems] = createSignal([]);
-	const [selectedTab, setSelectedTab] = createSignal();
+const [items, setItems] = createSignal([], { ownedWrite: true });
+const [selectedTab, setSelectedTab] = createSignal(undefined, { ownedWrite: true });
 	const { DomCollectionProvider } = createDomCollection({
 		items,
 		onItemsChange: setItems

diff --git a/dist/alert-dialog/DqopKvhq.jsx b/dist/alert-dialog/DqopKvhq.jsx
--- a/dist/alert-dialog/DqopKvhq.jsx
+++ b/dist/alert-dialog/DqopKvhq.jsx
@@ -23 +23 @@
-const AlertDialog = Object.assign(DialogRoot, {
+const AlertDialog = Object.assign((props) => <DialogRoot {...props} />, {

diff --git a/dist/alert-dialog/B8GtJUah.js b/dist/alert-dialog/B8GtJUah.js
--- a/dist/alert-dialog/B8GtJUah.js
+++ b/dist/alert-dialog/B8GtJUah.js
@@ -24 +24 @@
-const AlertDialog = Object.assign(DialogRoot, {
+const AlertDialog = Object.assign((props) => createComponent(DialogRoot, props), {
