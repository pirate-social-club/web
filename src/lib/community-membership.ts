// V1 supports only immediate open joins and gated access. Request-to-join stays
// out of the shared UI type until the product flow and API contract are real.
export type CommunityMembershipMode = "open" | "gated";
